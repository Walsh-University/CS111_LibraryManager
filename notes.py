#Main GitHub repo:
#https://github.com/Walsh-University/CS111_LibraryManager

#importing:
import math
from math import sqrt

################################## Classes ##################################
# class definition
class myClass:
class derivedClass(myClass):

# private attributes (Encapsulation):
#preface with an underscore - e.g., self._attributeName

# class properties - read (Encapsulation):
@property
def attributeName(self):
    return self._attributeName
    
# class properties - write (Encapsulation):
@attributeName.setter
def attributeName(self, value):
    self._attributeName = value
    
        
################################## Methods ##################################     
#function definition
def myFunctionDefinition():
def myFunctionWithParameters(parameter1, *args, **kwargs):
    #  *args is a tuple of zero-to-many positional arguments
    #  **kwargs is a dictionary of zero-to-many keyword arguments

myList = []
myDictionary = {}

# to use a class:
variableName = myClassDefinition()
class.method()

# Handle Errors:
RaiseError("Some Error Message")
try:
    # code that may cause an error
except exception as e:
    # code to handle the error
finally:
    # code that will always run to clean up resources opened in the try block
  
    
    
    
    
    
    
    
    